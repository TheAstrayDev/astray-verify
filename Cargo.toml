[package]
name = "astray-verify"
version = "0.1.0"
edition = "2021"
description = "Record and replay MCP server contracts"
license = "MIT"

[dependencies]
anyhow = "1"
clap = { version = "4", features = ["derive"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
